public class Demo{
    public static void main(String[] args) {
        // Fixed / static arrays -> Language biltin support

        /*
         * Syntax
         * new DataType[size];
         * new int[5];          -> int[]        -> datatype
         * new char[10];        -> char[]
         * new double[3];
         * new Calander[10];
         * new long[0] -> can create but cannot store data
         * 
         */

        // Dynamic arrays -> In the Java SE library
    }
}